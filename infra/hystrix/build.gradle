version = '0.0.1'

mainClassName = "com.epam.hw.netflix.HystrixServerApplication"

dependencies {
    compile libpacks.spring_common
    compile libpacks.cloud_config_client
    compile libpacks.cloud_histrix_client
}